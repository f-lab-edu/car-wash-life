# Car Wash Life Codex Guide

## 기본 원칙

- 작업을 시작하기 전에 관련 코드와 기존 구현을 먼저 확인한다.
- 확인하지 않은 코드의 동작을 추측하지 않는다.
- 요구사항에 없는 기능을 임의로 추가하지 않는다.
- 요청 범위를 벗어난 리팩터링을 하지 않는다.
- 새로운 외부 의존성을 임의로 추가하지 않는다.

## 설계 원칙

- 객체와 모듈을 설계하거나 변경할 때 SOLID 원칙을 준수한다.
- 하나의 클래스나 메서드가 서로 다른 변경 이유를 갖지 않도록 책임을 분리한다.
- 기능 확장마다 기존 코드의 반복적인 수정이 필요한 구조라면 변경 지점을 분리할 수 있는 설계를 검토한다.
- 하위 타입은 상위 타입이 보장하는 계약을 깨뜨리지 않아야 한다.
- 사용하지 않는 기능에 의존하도록 큰 인터페이스를 설계하지 않는다.
- 의존성 역전이 필요한 경계에서는 상위 수준의 정책이 구체적인 구현 세부사항에 직접 의존하지 않도록 한다.
- SOLID 준수를 이유로 필요하지 않은 인터페이스, 계층 또는 추상화를 미리 추가하지 않는다.
- 설계 원칙 간 충돌이 있거나 적용 여부가 불분명한 경우, 원칙을 기계적으로 적용하지 않고 현재 요구사항과 변경 가능성을 기준으로 판단한다.

## 패키지 및 타입 구성

- 기능 코드는 `application/{feature}` 아래에 `api`, `domain`, `service`로 구분한다.
- Controller는 `api`에 두고, API 요청·응답 DTO는 `api/dto`에 둔다.
- 한 기능의 요청과 응답이 한 쌍이면 `LoginDto.Request`, `LoginDto.Response`처럼 기능 단위 외부 클래스와 중첩 타입으로 묶는다.
- DTO 외부 클래스는 인스턴스화를 막는 private 생성자를 둔다. 상속을 막기 위한 `final`은 사용하지 않는다.
- ArgumentResolver는 인증 사용자처럼 여러 API에서 반복적으로 해석하는 값이나 복잡한 변환이 있을 때만 도입한다. 단순 RequestBody 바인딩을 위해 만들지 않는다.
- JPA Entity는 `infrastructure/persistence/entity`에 두며 `*Entity`로 명명한다. `domain` 패키지에 JPA Entity를 두지 않는다.
- Spring Data Repository는 `infrastructure/persistence`에 둔다.
- 별도 도메인 모델은 비즈니스 규칙과 영속성 모델의 변경 이유가 실제로 달라질 때 분리한다. 계층 분리만을 목적으로 Entity와 동일한 도메인 모델 및 매핑 코드를 미리 만들지 않는다.
- API, service, persistence 간 타입 의존 방향을 변경할 때는 관련 호출부와 매핑 책임을 함께 검토한다.

## 인증 기능 불변조건

- 회원가입 시 이메일과 비밀번호는 필수이고, 비밀번호 원문은 저장하지 않는다.
- 이메일 인증은 현재 범위에서 인증 완료 상태로 저장한다.
- 로그인 실패 응답으로 이메일 존재 여부나 계정 정보를 노출하지 않는다.
- AccessToken의 유효기간은 24시간이며 RefreshToken은 발급하거나 저장하지 않는다.
- AccessToken의 사용자 식별 claim에는 `userId`만 저장한다. 만료 검증을 위한 표준 메타데이터는 허용한다.
- 인증 필터가 검증한 `userId`는 Spring Security의 인증 principal로 제공한다.
- 토큰 서명 키와 데이터베이스 접속 정보는 설정으로 주입하고 코드에 운영 값을 하드코딩하지 않는다.

## 변경 및 검증

- 기존 동작을 변경하는 경우 관련 호출부와 영향 범위를 확인한다.
- 코드 변경 후 변경 범위와 관련된 테스트를 실행한다.
- 비즈니스 규칙을 추가하거나 변경한 경우 해당 동작을 검증하는 테스트를 작성한다.
- 테스트를 실행하지 못했거나 실패한 경우 이를 명확하게 보고한다.
- 테스트를 통과시키기 위해 요구사항이나 기존 테스트의 의도를 임의로 변경하지 않는다.

## 테스트

### 테스트 범위

- `domain`은 Spring Context 없이 단위 테스트한다.
- 단순 JPA Repository는 별도로 테스트하지 않는다.
- QueryDSL 등 직접 작성한 조회 로직은 persistence 테스트를 작성한다.
- `application`은 하위 레이어를 모킹한 단위 테스트를 기본으로 하지 않고, 실제 Spring Bean과 테스트용 데이터베이스를 연결한 통합 테스트를 작성한다.
- `api`는 service를 모킹한 Controller 단위 테스트를 기본으로 작성하지 않는다.
- 회원가입, 로그인, 토큰 인증 같은 주요 API 흐름은 `@SpringBootTest`와 MockMvc를 사용하는 API 통합 테스트로 검증한다.
- ArgumentResolver, Interceptor, 복잡한 요청·응답 직렬화처럼 API 계층 자체의 검증이 필요한 경우에만 Controller slice 테스트를 작성한다.
- 인증 테스트는 정상 발급뿐 아니라 잘못된 자격 증명, 비활성 계정, 만료시간, 토큰에서 복원되는 principal을 검증한다.
- 외부 시스템과의 실제 통신은 테스트에서 격리한다.

### 테스트 작성 원칙

- 테스트는 구현 세부사항보다 외부에서 관찰 가능한 동작과 비즈니스 규칙을 검증한다.
- 하나의 테스트는 하나의 시나리오를 검증하며, 해당 시나리오의 결과를 확인하기 위한 여러 assertion은 허용한다.
- 테스트 코드는 재사용성보다 가독성과 명확성을 우선한다.
- 테스트만을 위한 불필요한 추상화, 상속 구조, 복잡한 헬퍼를 만들지 않는다.
- 테스트 데이터는 테스트의 의도를 이해하는 데 필요한 값만 명시한다.
- 테스트의 준비 조건, 실행 행위, 기대 결과가 코드에서 명확하게 드러나도록 작성한다.
- 중복 제거가 테스트의 의도를 숨긴다면 공통화하지 않는다.
- 내부 메서드 호출 횟수나 구현 순서보다 최종 상태, 반환값 등 observable behavior를 우선 검증한다.
- 프로덕션 코드의 설계 원칙을 테스트 코드에 기계적으로 적용하지 않는다. 테스트 코드는 단순성과 명확성을 우선한다.

## 커밋

- 작업을 완료하고 관련 검증이 성공하면 변경사항을 커밋한다.
- 하나의 커밋에는 하나의 논리적인 변경만 포함한다.
- 하나의 작업에 서로 독립적인 변경이 포함된 경우 논리적인 단위로 커밋을 분리한다.
- 커밋 메시지는 한글로 작성하며 `<type>: <변경 내용>` 형식을 따른다.
- `type`은 다음 중 하나를 사용한다.
    - `feat`: 새로운 기능
    - `fix`: 버그 수정
    - `refactor`: 기능 변경 없는 리팩터링
    - `test`: 테스트 추가 또는 수정
    - `docs`: 문서 변경
    - `chore`: 빌드, 설정 등 기타 변경
- 테스트 또는 검증이 실패한 상태에서는 커밋하지 않는다.
